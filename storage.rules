rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read/download storage files
    match /{allPaths=**} {
      allow read: if true;
      // Only authenticated admin users can upload or delete storage files
      allow write: if request.auth != null;
    }
  }
}