rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read file metadata
    match /files/{fileId} {
      allow read: if true;
      // Only authenticated admin users can create, update, or delete files
      allow write: if request.auth != null;
    }
  }
}